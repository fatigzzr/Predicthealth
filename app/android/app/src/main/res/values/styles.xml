<?xml version="1.0" encoding="utf-8"?>
<resources>
    <style name="AppTheme" parent="Theme.MaterialComponents.DayNight.NoActionBar">
        <item name="android:colorBackground">@color/bg_dark</item>
        <item name="android:textColor">@color/input_text</item>
        <item name="android:editTextBackground">@color/input_bg</item>
    </style>

    <style name="InputStyle">
        <item name="android:backgroundTint">@color/input_bg</item>
        <item name="android:textColor">@color/input_text</item>
        <item name="android:textColorHint">@color/input_text</item>
        <item name="android:textColorPrimary">@color/input_text</item>
        <item name="android:padding">10dp</item>
        <item name="android:layout_marginBottom">10dp</item>
    </style>

    <style name="CardSection">
        <item name="cardCornerRadius">12dp</item>
        <item name="cardElevation">6dp</item>
        <item name="cardBackgroundColor">@color/panel_dark</item>
        <item name="android:layout_marginBottom">16dp</item>
    </style>

</resources>